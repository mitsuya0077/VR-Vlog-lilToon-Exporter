# サイトの画像

ページで使用する `vr-vlog-phone-framed.png` は、下記のスクリーンショットを承認済みのスマートフォン本体の枠に合成した画像です。画面の縦横比を保って縮小し、画面とアバターの再生成はしていません。枠の内側に残っていた以前の表示は除去し、不透明部分の全画素が縮小後のスクリーンショットと一致することを確認しています。Display P3カラープロファイルを保持しています。

`vr-vlog-screenshot-ocean-background.png` は、ユーザー提供の実機スクリーンショットの背面カメラ背景だけを、別に作成した海の画像へ置き換えた画像です。アバター、文字、操作ボタン、サムネイルは元のスクリーンショットの画素を使用しています。実際の撮影場所を示す画像ではありません。

アバターやUIを生成AIで再描画・補完しないでください。今後もスクリーンショットを土台にして、背景部分への合成で編集します。今回の海の背景だけは独立した生成画像です。スクリーンショットとアバターは生成ツールへ渡していません。

画面の945×2048のサイズとDisplay P3カラープロファイルを維持しています。内カメラは、ユーザーが明るさを調整して撮り直した画像の `(466, 337)` から `(916, 939)`（右・下の座標は含まない）を同じ位置・同じサイズで合成しています。こちらでは明るさ・色の補正、再生成を行っていません。角丸の外縁だけをマスクし、完全保持した270,372画素とアバター内部が新しい提供画像のRGB値と一致することを確認しました。内カメラ以外の画面と端末フレームは前の版を維持しています。

操作画面の元画像SHA-256: `453846eba796729cfcd64ba8786f9ba6795ac25acf60f43ae876ffbf30b630eb`

更新した内カメラの元画像SHA-256: `a26c281bd88ab66ae174d5147931f9905ec7dab30d158e5093cafd11fa4d1da2`
