# 変更履歴

## 0.10.1

160MiBを超えるファイルの読み込みには、160MiB制限の解除に対応したVR Vlogが必要です。先行配布するTestFlightの更新内容を確認してください。schema 2.0と標準MToon表示の互換性は維持します。

- 160MiB固定の書き出し制限を解除しました。GLBの整数範囲、破損データ、画像展開量などの検査は維持します。使用可能なメモリやGLB読み込み処理の配列上限による制約は残ります。
- 同じバイト列の保存領域を共有し、ファイル容量を減らします。ハッシュが一致した後に全バイトを照合し、読み込み中にSparse accessorが変更する基底領域は共有しません。
- 画像の各ミップ・Cubemap面は採取時にDeflate圧縮し、BINへの一括追記と直接書き込みで大きなメモリコピーを減らします。
- この最適化による画像縮小、ミップ省略、精度変更、表情・材質・機能の削減はありません。元アセットと従来の標準MToon互換表示は維持します。容量の削減率はモデル内の重複量により異なります。

## 0.10.0

新しい専用データの表示には、schema 2.0に対応したVR Vlogが必要です。対応アプリは先行してTestFlightへ配布します。アプリの更新内容を確認してからご利用ください。全機能のiPhone実機比較はまだ完了しておらず、Unityとの完全一致を保証するリリースではありません。

### lilToon専用データ

- 公式lilToon 2.3.4の型付きプロパティ、画像・ミップ・サンプラー、描画パスの有効状態を保存します。専用データは材質の焼き込み前に採取します。
- 専用画像の原寸とHDRを保持し、UV0〜7と元頂点IDを追加します。同名材質はglTFの材質・node・primitiveのインデックスで区別します。
- ファー・屈折・宝石・テッセレーションを含む公式シェーダー種別と設定を保存します。AudioLinkなどの外部連携、独自改造シェーダー、VRChatワールドの移植は対象外です。
- 既存VRMで失われた情報は復元できません。新しい設定を反映するには元のUnityアバターから再書き出ししてください。
- 専用データは原寸画像を保持するため、ファイルサイズとアプリの使用メモリが増える場合があります。対応アプリで資源が足りない場合は、専用表示の読み込みをエラーで停止します。

### 既存の標準MToon書き出し

- 半透明・カットアウトのメインカラー2nd/3rdで、静的な透明度の置換・乗算・加算・減算を色と一緒に焼き込み、標準MToon表示で使えるようにします。専用データは焼き込み前に別途保存します。
- 対象は固定UV0で、メイン画像の配置を適用したUVが0〜1に収まるレイヤーです。対応するデカール・MSDF・ブレンドマスクを反映し、その後にアルファマスクを合成します。動く模様、視点・表裏・距離で変わる設定は引き続き対象と理由を表示します。
- 元のアバターからの再書き出しが必要です。既存のVRMに省略された透過を後から復元することはできません。
- 確認済みの撫でシステムと、代替表示で非表示を指定した未対応シェーダーの補助Rendererを、自動で出力から除外します。
- 検出理由と「この対象は含める」の設定を追加しました。通常材質との混在や不明な対象は保持し、必要なら手動で除外できます。
- 衣装が使う骨や子オブジェクト、対応する表情を保持し、除外したメッシュへのVRM参照を出力用コピーで整理します。

## 0.9.0

追加表示には対応版のVR Vlogが必要です。0.9.0の追加データに未対応のアプリでは、標準MToon表示になります。以前のVRMに新しい設定を反映するには、元のアバターから再度書き出してください。

- MAで調整した現在の表示・体形・材質・Mesh Cutterの状態を保存できるようになりました。
- 静的アルファマスク、輪郭幅、発光の対応範囲を広げました。
- 発光を抑える2つの設定の初期値をオフへ変更しました。
- 書き出し結果に、近似・省略された内容を確認する案内を追加しました。
- 導入・操作説明とライセンス案内を整理しました。

## 0.8.1

- MA Bone Proxy / Merge Armatureで設定した髪・衣装の接続を、追加の手動選択なしで反映します。
- 独立した小物の接続を保ち、書き出し時の不要な確認を減らしました。

## 0.8.0

- lilToonの照明・影・リム・MatCapの対応範囲を広げ、互換表示を改善しました。
- 髪や小物の接続確認を追加し、MA関連の書き出しエラーを改善しました。

## 0.7.4

- MAの髪・衣装の接続と、書き出したモデルの形状を改善しました。
- 出力画像を縦横比を保って長辺1024以下へ縮小します。

## 0.7.3

- 対応するデカール・装飾・MSDF文字を書き出し画像へ反映します。
- 未対応設定の一覧と対処案内を追加し、指定したレイヤーを省略して再試行できるようにしました。

## 0.7.2

- 0.7.1の導入後にUnityで発生するコンパイルエラーを修正しました。

## 0.7.1

- 対応する重ね画像・色調補正とFaceEmoの登録内容を保存します。
- 別画像による強い発光の抑制と、不要なペット・ギミックの除外設定を追加しました。

## 0.7.0

- 対応する動く表情の書き出しを追加しました。再生には対応版のVR Vlogが必要です。
- 書き出し時に表情を自動で読み込み、操作画面を簡略化しました。

## 0.6.1

- 対応するVRChatジェスチャー表情を読み込みます。
- 表情数の表示と、書き出し完了画面を改善しました。

## 0.6.0

- 対応するVRChat Expressions MenuのButton・Toggle表情を読み込みます。
- 書き出せない表情の案内を追加しました。

## 0.5.1

- MatCap・輪郭線の互換表示を改善しました。
- 口の表情で認識できる名称を追加しました。

## 0.5.0

- BlendShapeで調整した顔・体形と、そこから切り替える表情の保存を改善しました。

## 0.4.1

- 非表示の衣装が原因で書き出しに失敗する問題を修正しました。

## 0.4.0

- 対応するまばたき・口の表情を補います。
- 影・輪郭線の互換表示を改善し、発光を抑える設定を追加しました。

## 0.3.9

- 輪郭線の太さと画像の扱いを修正しました。

## 0.3.8

- 同じ名前の画像がある場合に書き出せない問題を修正しました。

## 0.3.7

- UniVRM 0.131で書き出した一部のVRM 1.0を読み込めない問題を修正しました。

## 0.3.6

- 一部のlilToonシェーダー名を認識できない問題を修正しました。

## 0.3.5

- 対応外のlilToon効果を近似して書き出し、省略した内容を案内します。

## 0.3.4

- バックライトの画像設定に対応しました。

## 0.3.3

- バックライトの色と強さに対応しました。

## 0.3.2

- 操作画面とエラー案内を日本語化し、書き出し操作を簡略化しました。

## 0.3.1

- 一部のUnity環境で発生する導入時のコンパイルエラーを修正しました。

## 0.3.0

- VCC・ALCOMによる導入と、VRM 1.0への一括書き出しに対応しました。

## 0.2.0

- Unityでの書き出し画面と、lilToon設定の保存を追加しました。

## 0.1.0-preview.1

- 初期プレビュー版。
